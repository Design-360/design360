# development:
#   adapter: async

development:
  adapter: redis
  url: redis://localhost:6379
  
test:
  adapter: async

production:
  adapter: redis
  url: ENV['REDISTOGO_URL']
  channel_prefix: app_production
